import { defineCollection } from 'astro:content';

// Empty config to prevent Astro from auto-generating collections
// Our content/ folder contains plain TypeScript data files, not Astro content collections
export const collections = {};
