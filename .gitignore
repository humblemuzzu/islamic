# ── Build output ──────────────────────────────────────────
dist/
.astro/
convex/_generated/

# ── Dependencies ─────────────────────────────────────────
node_modules/
.convex/

# ── Logs ─────────────────────────────────────────────────
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
ocr_run.log
phase2_run.log

# ── Environment variables ────────────────────────────────
.env
.env.production
.env.local
.env.convex.local
.env.convex.*.local
scripts/ocr/.env
scripts/ocr/.venv/
scripts/chunking/.venv/

# ── Large files (see README.md → "Large Files" section) ──
# Source PDFs — 560MB+, download separately or use Git LFS
resources/

# Chunk + embedding artifacts — 1.2GB, regenerated from extracted/
chunks/all-chunks.json
chunks/all-chunks-embedded.json
chunks/all-chunks-embedded.jsonl
chunks/all-chunks-embedded-mistral-backup.json
chunks/all-chunks-embedded-mistral-backup.jsonl
chunks/embedding-progress.jsonl
chunks/embedding-progress-mistral-backup.jsonl

# Temp pipeline artifacts — fully regenerated by scripts
pdf_chunks/
ocr_artifacts/

# ── Python ───────────────────────────────────────────────
__pycache__/
*.pyc

# ── OS / Editor ──────────────────────────────────────────
.DS_Store
.idea/

# ── Misc ─────────────────────────────────────────────────
random.md
